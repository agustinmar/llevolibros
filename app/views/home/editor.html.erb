<div class="panel panel-primary">
  <div class="panel-heading">
    <h3 class="titgde panel-title">Acerca de la editorial</h3>
  </div>
  <div class="panel-body">
    <div class="row">
      <div class="col-md-4 text-center">
        <% if @editor.logo %>
          <%= link_to image_tag("editores/"+@editor.logo), @editor.web, target: "_blank" %>
        <% end %>
      </div>
      <div class="col-md-8">
        <p class="more titext text-justify"><%= @editor.descripcion %></p>
      </div>
    </div>
    <br>
    <% i = 1 %>
    <div class="row">
      <h4 class="titgde text-danger text-center">Libros de la editorial</h4>
      <% @libros.each do |libro| %>
        <% if (libro.editor == @editor) %>
          <% if (i <= 6) %>
            <div class="col-sm-6 col-md-2">
              <div class="thumbnail">
                <%= link_to image_tag("portadas/"+libro.portada, :class=>"img-responsive"), home_libro_path(:id => libro.titulo) %>
                <div class="caption">
                  <h5 class="media-heading">
                    <%= link_to libro.titulo, home_libro_path(:id => libro.titulo), :class=>"titlibro" %>
                  </h5>
                  <h6 class="media-heading">
                    <% libro.titulo.autor.each do |aut| %>
                      <%= link_to aut, home_autor_path(:id => aut), :class=>"titautor" %><% if !(aut == libro.titulo.autor.last) %>,<br><% end %>
                    <% end %>
                  </h6>
                </div>
              </div>
            </div>
          <% else %>         
            </div>
            <div class="row">
            <% i = 1 %>
            <div class="col-sm-6 col-md-2">
              <div class="thumbnail">
                <%= link_to image_tag("portadas/"+libro.portada, :class=>"img-responsive"), home_libro_path(:id => libro.titulo) %>
                <div class="caption">
                  <h5 class="media-heading">
                    <%= link_to libro.titulo, home_libro_path(:id => libro.titulo), :class=>"titlibro" %>
                  </h5>
                  <h6 class="media-heading">
                    <% libro.titulo.autor.each do |aut| %>
                      <%= link_to aut, home_autor_path(:id => aut), :class=>"titautor" %><% if !(aut == libro.titulo.autor.last) %>,<br><% end %>
                    <% end %>
                  </h6>
                </div>
              </div>         
            </div>
          <% end %>
          <% i += 1 %> 
        <% end %>
      <% end %>
    </div>
  </div>
</div>